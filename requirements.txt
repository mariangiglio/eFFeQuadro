# eFFeQuadro - Forensic Toolkit
# Requires Python 3.10 or later.
#
# tkinter is NOT listed here: it is part of the standard library, but on some
# Linux distributions it must be installed separately (see README).

ttkthemes>=3.2.0
pandas>=1.5.0
matplotlib>=3.6.0
